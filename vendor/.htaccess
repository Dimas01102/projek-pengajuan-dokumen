# =============================================
# VENDOR FOLDER - COMPLETE PROTECTION
# =============================================

# Blokir SEMUA akses ke folder vendor
<IfVersion < 2.4>
    Order Deny,Allow
    Deny from all
</IfVersion>
<IfVersion >= 2.4>
    Require all denied
</IfVersion>

Options -Indexes